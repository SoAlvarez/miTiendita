const NavBar = () => {
    return (
        <nav>
            <a href="#">Inicio</a>
            <a href="#">Productos</a>
            <a href="#">Contacto</a>
        </nav>
    );
}

export default NavBar;