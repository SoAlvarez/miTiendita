import CartWidget from "./CartWidget";

const NavBar = () => {
    return (
        <><nav>
            <a href="#">Inicio</a>
            <a href="#">Productos</a>
            <a href="#">Contacto</a>
        </nav> <CartWidget />7</>
    );
}

export default NavBar;