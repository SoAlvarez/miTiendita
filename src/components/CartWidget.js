import {CiShoppingCart} from 'react-icons/ci';

const CartWidget = () => {
  return (
    <div className="cart">
      <CiShoppingCart></CiShoppingCart>
    </div>
    );
};

export default CartWidget;